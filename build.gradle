apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "numToWord"
version = '1.0'
mainClassName = "NumToWord"

repositories {
  mavenCentral()
}

dependencies {
  compile group: 'junit', name: 'junit', version: '4.+'
}
